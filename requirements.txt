fastapi>=0.112.0
uvicorn[standard]>=0.30.0
pydantic>=2.7.0
pydantic-settings>=2.4.0
PyYAML>=6.0.1
transformers>=4.43.0
accelerate>=0.33.0
datasets>=2.20.0
tokenizers>=0.15.2
sentencepiece>=0.2.0
peft>=0.11.0
trl>=0.9.4

# torch is environment-specific; install a CUDA/CPU build separately
# bitsandbytes is optional for 4-bit; install where supported